export interface ProfileDetails {
    publicSigningKey: string;
    email: string;
    fullName?: string;
    phone?: string;
    avatarUrl?: string;
    avatarTx?: string;
    avatar?: ArrayBuffer;
  }
