name: Test akord-js methods on comment '/test test'

on: 
  issue_comment:
    types: [created, edited]

jobs:
  test:
    runs-on: ubuntu-latest
    if: github.event.issue.pull_request
    steps:
      - name: Run test on comment
        run: echo test